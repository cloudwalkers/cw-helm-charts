apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cloudwalker-aws.fullname" . }}-test-connection"
  labels:
    {{- include "cloudwalker-aws.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cloudwalker-aws.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
