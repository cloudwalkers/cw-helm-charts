apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloudwalker-aws.fullname" . }}
  labels:
    {{- include "cloudwalker-aws.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cloudwalker-aws.selectorLabels" . | nindent 4 }}
